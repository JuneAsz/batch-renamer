Batch renamer in Go

build with:
go build -o rena

run:
rena -p path/to/folder -n newname

example:
rena -p ./testDir -n MyFile

prints renamed files and result if you pass -print flag

needs Go installed (loll)

(im noob)
